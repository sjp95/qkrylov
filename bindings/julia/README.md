# QKrylov.jl

This directory is reserved for the Julia interface and Julia package metadata.

Current status: scaffold only. The package does not yet bind to the C++ core.

Expected future options:

- Wrap the C++ core through CxxWrap.jl.
- Ship prebuilt binaries through JLL packages.
- Provide pure Julia user-facing convenience APIs on top of the compiled core.

